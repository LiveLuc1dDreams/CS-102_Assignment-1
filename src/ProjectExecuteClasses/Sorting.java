package ProjectExecuteClasses;

import ProjectObjects.Course;
import ProjectObjects.Student;

/**
 * Created by brandon on 4/8/17.
 * This class stores the various sorting methods that will be used to sort the Student and Course database arrays
 * before they are searched with search methods from UniversityDatabase.java
 */
public class Sorting {

    //method to sort Students in StudentDatabase by their unique ids (from highest to lowest)
    public static void sortStudentsById(Student StudentDatabase[]) {

    }

    //method to sort Courses in CourseDatabase by their unique ids (from highest to lowest)
    public static void sortCoursesById(Course CourseDatabase[]) {

    }

    //method to compare the Id's from StudentDatabase sorting method
    public static int compareStudentIds(Student StudentDatabase[]) {
        int result;
        //check if the student

        return result;
    }

    //method to compare the Id's from CourseDatabase sorting method
    public static int compareCourseIds(Course CourseDatabase[]) {
        int result;

        return result;
    }
}
